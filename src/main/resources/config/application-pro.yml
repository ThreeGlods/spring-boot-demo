server:
  port: 80
  servlet:
    context-path: /


logging:
  level:
    root: warn
    com.example: info
  file: logs/my.log
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jyt?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: jx@19940228
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false